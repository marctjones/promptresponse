# Packages the specification and conformance scripts need.
#
# These scripts import no SDK — that independence is the point, since they are
# what an SDK is measured against — so their dependencies are declared here
# rather than borrowed from python/pyproject.toml. The versions match it, and
# cel-python matches the CEL implementation the specification pins.
#
#   python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
#
# A gate whose package is missing exits non-zero saying so; it never reports the
# absence as a defect in a document.

PyYAML==6.0.3          # APR-YAML syntax; scalar resolution is the spec's own
jsonschema==4.26.0     # check-schema.py
cel-python==0.5.0      # expression hints, the version section 11.3 pins
asn1crypto>=1.5.1      # CMS SignedData in the attestation fixtures
cryptography>=50.0.1   # ECDSA P-256 signing for those fixtures
