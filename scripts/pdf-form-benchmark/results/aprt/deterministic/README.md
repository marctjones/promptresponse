# Deterministic pipeline output

`convert-pdf` run over the eleven scored corpus forms, kept so
`score_deterministic.py` can put the deterministic pipeline on the same axis as
the vision models. Regenerate with the loop in that script's docstring.

Not a model. The directory sits under `results/aprt/` because that is where the
scorer looks for a run's output, not because a model produced it.
