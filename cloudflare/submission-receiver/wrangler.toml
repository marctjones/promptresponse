name = "apr-submission-receiver"
main = "worker.js"
compatibility_date = "2024-11-01"

# A stateless demo receiver -- see worker.js's own header comment for the full
# contract. Compatibility date is pinned the way a package version would be,
# so a future Workers runtime change is a deliberate bump, not a silent drift.

[[kv_namespaces]]
binding = "SUBMISSIONS"
id = "8fc50ee8c60942ca91c0a784b6666724"
