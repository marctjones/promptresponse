# Core library has no required dependencies
# Responses are stored as strings, JSON serialization uses stdlib

# Optional: Digital signature support
cryptography>=41.0.0

# Development dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
mypy>=1.5.0
