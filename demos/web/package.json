{
  "name": "promptresponse-browser-demo",
  "private": true,
  "version": "1.0.0-beta.3",
  "type": "module",
  "scripts": {
    "build": "npm --prefix ../../typescript run build && tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2"
  }
}
